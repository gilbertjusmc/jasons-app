import React from 'react'

const UserContext = React.createContext({})

export const UserProvider = UserContext.UserProvider
export const UserConsumer = UserContext.UserConsumer

export default UserContext